<form method="post">
    <section class="hero is-primary">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                Skapa kår
            </h1>
            <h2 class="subtitle">
                Kom igång genom att skapa en kår.
            </h2>
        </div>
    </div>
</section>
<section class="section">
    <div class="container">
        <div class="content">
            <p class="subtitle">Vad heter din scoutkår?</p>

            <input class="input is-large"
                   type="text"
                   id="new_organization_name"
                   name="new_organization_name"
                   placeholder="Namn på scoutkår"/>
        </div>
        <div class="content">
            <p class="subtitle">Hur vill du logga in?</p>

            <p>Genom att logga in så skapas ditt konto och din kår. Du kommer samtidigt att bli administratör för
                kåren.</p>
        </div>

        <div class="columns">
            <div class="column">
                <article class="notification">
                    <div class="field">
                        <div class="control">
                            <button class="button is-primary is-medium" type="submit"
                                    formaction="{{host}}/api/auth/google/signup">
                                Logga in med Google
                            </button>
                        </div>
                    </div>
                    <div class="content is-small">
                        <p>Det här passar dig som har en Gmail-adress.</p>
                    </div>
                </article>
            </div>
            <div class="column">
                <article class="notification">
                    <div class="field">
                        <div class="control">
                            <button class="button is-primary is-medium" type="submit"
                                    formaction="{{host}}/api/auth/microsoft/signup">
                                Logga in med Microsoft
                            </button>
                        </div>
                    </div>
                    <div class="content is-small">
                        <p>Det här passar dig som har en Outlook-, Hotmail- eller Live-adress.</p>
                    </div>
                </article>
            </div>
            <div class="column">
                <article class="notification">
                    <p class="subtitle">...eller din e-postadress</p>
                    <div class="field">
                        <div class="control">
                            <input class="input" type="email" id="email" name="email" placeholder="Din e-postadress">
                        </div>
                    </div>

                    <div class="field">
                        <button class="button is-primary is-medium" type="submit"
                                formaction="{{host}}/api/auth/email/signup">
                            Logga in med e-post
                        </button>
                    </div>

                    <div class="content is-small">
                        <p>Du kommer att få ett e-post med en länk för att skapa kontot.</p>
                    </div>
                </article>
            </div>
        </div>
        <p>Nej, det här var inte riktigt min grej. Jag vill gå till <a href="#" aria-current="page">startsidan</a>
            istället.
        </p>
    </div>
</section>
</form>
