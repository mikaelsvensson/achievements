<section class="hero is-primary">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                Det gick inte att logga in
            </h1>
            <h2 class="subtitle">

            </h2>
        </div>
    </div>
</section>
<section class="section">
    <div class="container">
        <div class="content">
            <p><strong>{{message}}</strong></p>
            <p>Vad sägs om att <a href="/">gå till startsidan</a>?</p>
            {{#if customerSupportEmail}}
            <p>Kontakta oss på <a href="mailto:{{customerSupportEmail}}">{{customerSupportEmail}}</a> om problemet inte
                försvinner.</p>
            {{else}}
            <p>Försök gärna en gång till.</p>
            {{/if}}
        </div>
    </div>
</section>