<section class="section">
    <div class="container">
        <h1 class="title">
            Min profil
        </h1>

        <div class="columns">
            <div class="column">
                <div class="content">
                    <p>Hej, {{person.name}}.</p>
                    <p>Din e-postadress är {{person.email}}.</p>
                    <p>Du är med i kåren <a href="#karer/{{organization.id}}">{{organization.name}}</a>.</p>
                    {{#each person.attributes}}
                    <p>{{key}}: {{value}}</p>
                    {{/each}}
                </div>
            </div>
            <div class="column">
                <div class="box">
                    <h2 class="subtitle">Märken</h2>
                    <div id="achievements-summary"></div>
                </div>
            </div>
        </div>
</section>
{{> ../_footer.handlebars}}